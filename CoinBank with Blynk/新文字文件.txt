#define BLYNK_TEMPLATE_ID "TMPL6yT9dVA4g"
#define BLYNK_TEMPLATE_NAME "Coin Bank"
#define BLYNK_AUTH_TOKEN "57W5Ehi30MIawC4XVUP9CJ4Er6yiXyea"

#define BLYNK_PRINT Serial

#include <ESP8266WiFi.h>
#include <BlynkSimpleEsp8266.h>

char ssid[] = "SC_iPhone";
char pass[] = "asdfghjkl";

BlynkTimer timer;

void sendData() {
  if (Serial.available() > 0) {
    Blynk.virtualWrite(V2, coinCount);
  }
}

void setup() {
  Serial.begin(115200);
  UnoSerial.begin(115200);      // Initialize software serial port to communicate with UNO
  Blynk.begin(BLYNK_AUTH_TOKEN, ssid, pass);
  // while (Blynk.connect() == false) {
  //   // Wait until connected
  // }
  timer.setInterval(1000L, sendData);  // Set a timer to send data every second
}

void loop() {
  Blynk.run();
  timer.run();
  
  if(Serial.available()>0) {
    bool recivedBool = Serial.read();
    Serial.println(recivedBool);
  }
}
